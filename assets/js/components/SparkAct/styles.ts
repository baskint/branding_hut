import styled from 'styled-components';
import { Controller } from 'react-hook-form';
import {
  Typography,
  TextField,
  InputLabel,
  Grid,
  Divider,
} from '@mui/material';

export const ActDateTime = styled(Grid)`
 .MuiFormControl-root {
    min-width: 248px;
  }
`;